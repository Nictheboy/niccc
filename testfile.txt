const int ident = 1;
int array[10] = {1, 2, 3, 4};

int add(int a, int b) {
    return a + b;
}

void nop() {}

int main(){
    int a, b, c, d, e, result = 0;
    a = getint();
    b = getint();
    c = getint();
    d = getint();
    e = getint();
    nop();
    e = -e + c * d - a % b + c / (a + b) + add(a, b);
    array[0] = e + ident;
    printf("%d, %d, %d\n", a, b, c);
    printf("%d\n", array[0] - ident);
    return 0;
} 